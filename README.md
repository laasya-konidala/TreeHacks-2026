# Learning overlay

Desktop overlay that sits above all apps. Menu bar only (no dock icon).

## Run

```bash
npm install
npm start
```

- **Menu bar:** Click the small icon to show or hide the gray overlay.
- **Overlay:** Gray translucent box with placeholder text. Stays on top of other apps.
- **Quit:** Right-click the menu bar icon → Quit.
